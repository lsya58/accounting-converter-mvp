[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "accounting-converter"
version = "0.1.0"
description = "Local accounting journal converter MVP"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
